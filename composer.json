{
  "name": "dagemawi/commission-calculation-engine",
  "description": "A validated, testable PHP commission engine for deposits, withdrawals, weekly allowances and currency conversion.",
  "type": "library",
  "license": "MIT",
  "require": {
    "php": "^8.1",
    "league/csv": "^9.8"
  },
  "require-dev": {
    "phpunit/phpunit": "^10.5"
  },
  "autoload": {
    "psr-4": {
      "CommissionApp\\": "src/"
    }
  },
  "scripts": {
    "lint": "find src tests -type f -name '*.php' -print0 | xargs -0 -n1 php -l",
    "test": "phpunit",
    "check": [
      "@lint",
      "@test"
    ]
  },
  "config": {
    "sort-packages": true
  }
}
